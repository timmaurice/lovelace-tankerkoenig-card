# Dependencies
/node_modules

# Build output — built fresh by the release workflow and shipped as a release asset
# (dist/stylesheet.css is hand-written and linked from the README, so it stays tracked)
dist/tankerkoenig-card.js
dist/tankerkoenig-card.js.map

# Lockfiles for other package managers
bun.lock
yarn.lock

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

/test/ha-config

.DS_Store

*.bak

# Playwright
test-results/
playwright-report/

# Local machine compose override (corporate CA, local paths)
docker-compose.override.yml
